name: Discord Commit Notifier

on:
  push:
    branches: [ main ]

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Get last 5 commits summary
        id: summary
        run: |
          COMMITS=$(git log -n 5 --pretty=format:"• %h: %s by %an")
          echo "COMMITS=$COMMITS" >> $GITHUB_ENV

      - name: Get last 5 commits detailed
        id: detailed
        run: |
          COMMITS_DETAILED=$(git log -n 5 --pretty=format:"• %h: %s by %an <%ae> on %ad")
          echo "COMMITS_DETAILED=$COMMITS_DETAILED" >> $GITHUB_ENV

      - name: Send Discord message with "Detailed View" button
        run: |
          CHANNEL_ID="1486579386239942676"

          curl -s -X POST "https://discord.com/api/v10/channels/$CHANNEL_ID/messages" \
            -H "Authorization: Bot ${{ secrets.DISCORD_BOT_TOKEN }}" \
            -H "Content-Type: application/json" \
            -d "{
              \"content\": \"📌 **${GITHUB_ACTOR}** pushed to **${GITHUB_REPOSITORY}**\",
              \"embeds\": [
                {
                  \"title\": \"Last 5 commits summary\",
                  \"description\": \"${COMMITS}\",
                  \"color\": 5814783,
                  \"fields\": [
                    {
                      \"name\": \"Detailed View\",
                      \"value\": \"Click the 'View Detailed Commits' button below\"
                    }
                  ]
                }
              ],
              \"components\": [
                {
                  \"type\": 1,
                  \"components\": [
                    {
                      \"type\": 2,
                      \"label\": \"View Detailed Commits\",
                      \"style\": 1,
                      \"custom_id\": \"show_details\"
                    }
                  ]
                }
              ]
            }"